// Game of Life  

let grid;
let gridSize = 10;


function setup() {
  createCanvas(windowWidth, windowHeight);
  grid = createEmpty2DArray(gridSize, gridSize);
}

function draw() {
  background(220);

}


function create2DArray(rows, cols) {
  for (let x; x<heightX; x++) {
    push [0];
  }
  for (let y; y<heightY; y++) {
    push [0]
  }
  


}
