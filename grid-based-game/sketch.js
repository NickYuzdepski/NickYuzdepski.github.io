// Grid Based Game
// Nicklas Yuzdepski
// October 20, 2021

let gridSize = 100;
let grid;

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);

  rect(mouseX, mouseY, 100, 50);
}
